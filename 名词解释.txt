原函数/原语:由系统提供的函数称之为原函数或原语。
函数名:函数名是一个符号,代表该函数的起始位置。
函数参数:函数参数是显示给函数进行处理的数据项。
局部变量:函数进行处理时使用的数据存储区域，在函数返回时被废弃。
静态变量:函数进行处理时使用的数据存储区域，但是在函数返回时不会被废弃，每当函数代码被激活时都重复使用。
全局变量:函数进行处理时用到，在函数之外管理的数据区域。
返回地址:是一个看不见的参数，应为它不是在函数中使用，返回地址告诉函数再其执行完当前代码后应该再从哪里开始执行。
